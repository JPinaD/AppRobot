<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- MQTT y NSD sobre red local (hotspot) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Bluetooth clásico para comunicación con hardware HC-05 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppRobot"
        tools:targetApi="31">

        <activity
            android:name=".ui.waiting.WaitingSessionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.pictogram.PictogramActivity"
            android:exported="false" />
        <activity
            android:name=".ui.sessionend.SessionEndActivity"
            android:exported="false" />
        <activity
            android:name=".ui.profile.ProfileSelectionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.message.RobotMessageActivity"
            android:exported="false" />
        <activity
            android:name=".ui.feedback.FeedbackActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.ActivityPlayerActivity"
            android:exported="false" />

        <activity
            android:name=".ui.splash.RobotSplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
