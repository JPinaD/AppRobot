<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppRobot" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppRobot" parent="Base.Theme.AppRobot" />

    <!-- Tema TEA: fondo claro, colores suaves, sin ActionBar -->
    <style name="Theme.AppRobot.TEA" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/tea_bg</item>
        <item name="colorPrimary">@color/tea_accent</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSurface">@color/tea_card_bg</item>
        <item name="colorOnSurface">@color/tea_text_primary</item>
        <item name="android:textColor">@color/tea_text_primary</item>
    </style>
</resources>